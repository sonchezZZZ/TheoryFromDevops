---
- name: Install default Apache
  hosts: all
  become: yes
  
  tasks:
  - name: Install Apache
    yum:  name=httpd state=latest
    
  - name: Start Apache
    service: name=httpd state=started enabled=yes
